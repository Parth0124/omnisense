// Dashboard page - scaffold only.
// signal volume, active investigations, top trends, connector health

export default function DashboardPage() {
  return (
    <main className="p-8">
      <h1 className="text-2xl font-semibold">Dashboard</h1>
      <p className="text-muted-foreground mt-2">TODO: signal volume, active investigations, top trends, connector health</p>
    </main>
  );
}
