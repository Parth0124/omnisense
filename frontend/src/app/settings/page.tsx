// Settings page - scaffold only.
// connectors, credentials, models, workspace preferences

export default function SettingsPage() {
  return (
    <main className="p-8">
      <h1 className="text-2xl font-semibold">Settings</h1>
      <p className="text-muted-foreground mt-2">TODO: connectors, credentials, models, workspace preferences</p>
    </main>
  );
}
