// EvidencePanel - scaffold only. TODO: retrieved passages backing the current step

export function EvidencePanel() {
  return <div data-testid="EvidencePanel">TODO: retrieved passages backing the current step</div>;
}
